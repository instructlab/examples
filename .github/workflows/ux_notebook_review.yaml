name: "UX Notebook Review GH Action"

on:
  workflow_dispatch:
    inputs:
      notebook-path:
        description: 'File path or glob for notebooks to review (relative to repo root)'
        required: false
        default: 'ux-notebook-reviewer/*.ipynb'

  pull_request:
    paths:
      - 'ux-notebook-reviewer/**'
      - '.github/workflows/ux_notebook_review.yml'

  push:
    branches:
      - ux-notebook-reviewer
    paths:
      - 'ux-notebook-reviewer/**'
      - '.github/workflows/ux_notebook_review.yml'

jobs:
  review:
    name: Run UX Review
    runs-on: ubuntu-latest

    steps:
      - name: Check out repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.11'

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install openai nbformat ipython ipywidgets

      - name: Run review script
        env:
          OPENAI_API_KEY: ${{ secrets.OPENAI_API_KEY }}
        run: |
          NOTEBOOK_PATH="${{ github.event.inputs.notebook-path }}"
          if [ -z "$NOTEBOOK_PATH" ]; then
            NOTEBOOK_PATH='ux-notebook-reviewer/*.ipynb'
          fi

          echo "Reviewing notebooks under path: $NOTEBOOK_PATH"
          python ux-notebook-reviewer/review-notebook-ghscript.py --path "$NOTEBOOK_PATH" > review.md

      - name: Upload review artifact
        uses: actions/upload-artifact@v4
        with:
          name: ux-notebook-review
          path: review.md

      - name: Comment on pull request
        if: github.event_name == 'pull_request'
        uses: peter-evans/create-or-update-comment@v4
        with:
          token: ${{ secrets.GITHUB_TOKEN }}
          issue-number: ${{ github.event.pull_request.number }}
          body: |
            ## Automated UX Notebook Review
            <details>
            <summary>Click to expand review output</summary>

            ```markdown
            $(sed 's/^/    /' review.md)
            ```
            </details>
